#include<stdio.h>
int main()
{
    int n,i,sum;
    sum=0;
    printf("enter how many numbers sum you want");
    scanf("%d",&n);
    for(i=1;i<=2*n-1;i+=2)
    {
        sum+=i;
    }
    printf("the sum of first %d odd numbers is %d",n,sum);
}
