//binary expansion of a number
#include <stdio.h>
void binary(int);
int main()

{
    int n;
    printf("Enter any number");
    scanf("%d",&n);
    printf("the binary expansion of %d is:\t",n);
    binary(n);
}
void binary(int n)
{
     if (n == 0)
        return;

    binary(n / 2);  
    printf("%d", n % 2);
    
}